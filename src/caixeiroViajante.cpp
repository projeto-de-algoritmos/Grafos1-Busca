#include <bits/stdc++.h>
using namespace std;
#define V 4 

int caixeiroViajante(int graph[][V], int s) {

    vector<int> vertice;
    for (int i = 0; i < V; i++)
        if(i!=s)
            vertice.push_back(i);
   
}

int main () {
    
    return 0;
}
